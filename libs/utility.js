const  errorPlaceHolder = (status, success, message, data) => ({
    status,
    success,
    message,
    data
});
const  placeHolder = (success, message, data) => ({
    success,
    message,
    data
});
